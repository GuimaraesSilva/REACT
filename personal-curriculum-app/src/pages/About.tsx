import React from 'react';

const About: React.FC = () => (
  <div>
    <h1>About This App</h1>
    <p>This is a simple task manager application built with React and TypeScript.</p>
  </div>
);

export default About;
